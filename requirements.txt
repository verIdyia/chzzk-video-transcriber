# Core dependencies
streamlit>=1.30.0
requests>=2.31.0
ffmpeg-python>=0.2.0
tqdm>=4.66.0

# Audio/ML dependencies (PyTorch installed separately in Dockerfile)
numpy>=1.24.0
scipy>=1.11.0
soundfile>=0.12.0
librosa>=0.10.0

# Whisper - faster-whisper (primary, 4x faster than openai-whisper)
faster-whisper>=1.1.0

# Speaker diarization - no HuggingFace token needed
wespeaker @ git+https://github.com/wenet-e2e/wespeaker.git
simple-diarizer

# Speaker diarization - optional (requires HuggingFace token)
# pyannote.audio>=3.1.0
